import "../Styles/DeliveriesTitle.scss";

const DeliveriesTitle = () => {
    return (
        <section className="deliveries-title">
            <h1>Доступные заказы</h1>
        </section>
    );
}

export default DeliveriesTitle;